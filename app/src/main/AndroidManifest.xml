<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.registromascotas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.RegistroMascotas">
        <activity
            android:name=".AccessActivity"
            android:exported="false" />
        <activity
            android:name=".SuccessActivity"
            android:exported="false" /> <!-- Pantalla de registro u logueo -->
        <activity
            android:name=".AuthActivity"
            android:exported="false" /> <!-- Pantalla principal (PRIMERA) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Pantalla de registro -->
        <activity
            android:name=".WelcomeActivity2"
            android:exported="false" /> <!-- Main por defecto -->
        <activity
            android:name=".CreateAccountActivity"
            android:exported="false" />
    </application>

</manifest>